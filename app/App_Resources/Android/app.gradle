// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.jonbwalker.hiittimer"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
